// Generated by CoffeeScript 2.5.1
var Coder, Protocol, Reqs, arr, i, len, modules, name, path,
  hasProp = {}.hasOwnProperty;

Reqs = require('./reqs');

Coder = require('./lib/Coder');

Protocol = require('./lib/Protocol');

modules = {
  coders: [],
  protocols: ['JRPCProtocol']
};

Reqs.require = function(path) {
  return Reqs.addModule(require(path));
};

for (path in modules) {
  if (!hasProp.call(modules, path)) continue;
  arr = modules[path];
  for (i = 0, len = arr.length; i < len; i++) {
    name = arr[i];
    Reqs.require(`./${path}/${name}`);
  }
}

module.exports = Reqs;

//# sourceMappingURL=index.js.map
